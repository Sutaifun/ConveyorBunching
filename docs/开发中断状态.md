# 开发状态（2026-09-13）

> 接手说明：读完本文 + 指导文第 4.7 节（结论）和第 4.9 节（播放器规格）就能继续，不需要回看聊天记录。
> 施工规格以 [`科学小品指导.md`](科学小品指导.md) 为准；本文只记「做到哪、下一步做什么」。

## 一句话状态

**数据、图、播放器都通了，只剩正文没写。** 六张图定稿在 `figures/`，播放器 `web/KettlePlayer.vue` 已实测可跑（`npm run play`），所有数字都是实测时标（一圈 450 秒）下跑出来的。

## 环境

| 项 | 状态 |
|---|---|
| Node | v22.11.0，`--experimental-strip-types` 直接跑 `.ts`，**不需要 tsx** |
| 运行时依赖 | 无。`npm run sim` / `check` 不必 `npm install` |
| devDependencies | 只为本地预览播放器。**注意 `vue-tsc` 不兼容 TypeScript 7**（Go 重写版不提供 `lib/tsc`），所以 `typescript` 钉在 `~5.9` |
| `.venv` | 已装 numpy 2.5.2 + matplotlib 3.11.1，**无需联网** |
| 中文字体 | `PingFang SC` 可用，`plot.py` 会自动挑；找不到会退成英文标签并打印警告 |
| git | `main` 跟踪 `origin/main`，远程 <https://github.com/Sutaifun/ConveyorBunching>（public） |

复现（都不联网，全程约 3 秒）：

```bash
npm run check                     # 自检
npm run sim                       # 六个场景 → data/*.json
npm run sim -- scene-a            # 只跑一个
./.venv/bin/python src/plot.py    # data/ → figures/
./.venv/bin/python src/plot.py reset   # 只画一张

npm install && npm run play       # 本地预览播放器
npm run play:build                # 类型检查 + 构建（改组件后跑一下）
```

## 现有文件

| 文件 | 作用 | 状态 |
|---|---|---|
| `src/sim.ts` | **唯一模拟核** + 随机对照线。事件驱动、mulberry32 可播种、无 Vue / DOM / Node 依赖 | 可用 |
| `src/units.ts` | 秒/分钟 ↔ 圈 换算，cli 与播放器**共用** | 可用 |
| `src/cli.ts` | Node 批处理，六个场景 → `data/*.json`，已用实测时标 | 可用 |
| `src/plot.py` | 只读 JSON 出图，**不含任何物理** | 可用，六张图定稿 |
| `figures/fig1..6` | 小品配图 | 已定稿，已入库 |
| `data/*.json` | 场景输出 | 已忽略（几秒可重跑） |
| `web/KettlePlayer.vue` | 播放器 | 可用，已在 Chrome 里实测各状态 |
| `web/index.html`、`web/main.ts`、`vite.config.ts` | 本地预览壳，不进博客 | 可用 |
| `essay/` | 小品正文 | **还没写** |

`src/diag.ts` 已删除，有用的断言并进了 `check`。

## 自检覆盖了什么

`npm run check` 会挡住这些错：同种子两次结果不一致（非确定性）、换种子结果不变（种子没接上）、均匀排布 CV≠0、全挤一点 CV≠√(K−1)、**空店却出现服务或结团**（核凭空造聚集）、结团时间随阈值非单调、`units.ts` 换算漂移、以及**播放器与文章跑出的历史不一致**（等待序列逐位比对）。

最后一条是复现承诺的兜底：读者拿文章图注里的种子，必须在小玩意里看到同一段开餐。别删。

## 必须保住的结论

数字的单一出处是**指导文第 4.7 节**，不在这里重复维护。三条骨干：

1. 一顿饭的长度刚好够结团走完全程——16 分钟越过「随便乱扔」，25 分钟半程，90 分钟时六把壶挤在圆周 2% 的弧段里。
2. 需求率只决定多快，不决定会不会：60 倍需求率变化 → 18 分钟对 281 分钟，终点一样。
3. 姜片（K=1）的热座效应没有空间范围，隔一桌就归零。这是「结团至少要两件东西」的反证。

## 未解决的问题

### P0：下一步

1. **写小品正文。** 这是现在唯一的主线。素材全齐：第 2 节八步叙事弧、第 4.7 节数字、`figures/` 六张图、第 7 节文献。第 4 步和第 5 步的说法已按数据改过，照现在的指导文写即可。

### P1

2. 图注要写上种子和参数，并和播放器的 URL query 对齐，读者才能复现。目前 `figures/` 里只有图，图注还没配。正文里每张图旁边最好直接给一条播放器链接（带 `t` 参数指向对应时刻）。
3. 播放器接进博客的路径还没验证：`StyphonBlog` 里 `/widgets` 由 `VITE_FEATURE_WIDGETS` 开关控制，`HomeWidgetBoard.vue` 的 widgets 数组是空的。新 monorepo 本地没有，实际接线要在那边做。搬过去只需要 `KettlePlayer.vue` + `src/sim.ts` + `src/units.ts`，`web/index.html`、`web/main.ts`、`vite.config.ts` 和 devDependencies 都是本地预览用的，不用跟去。
4. 播放器目前播到缓冲末尾就停，不追加计算（规格允许）。若正文需要更长的预演，把「预演多长时间」调大即可，但帧缓冲会线性变大。

### P2

5. 容量与回壶（10 次、过原点且 ≤40% 用 3τ 补汤）仍是关着的开关，见指导文第 5 节。第一版小品不等它。
6. 播放器文案是硬编码中文，接进博客时再补 i18n。

## 数据缺口

- **回转火锅的带速没有实测**，目前借用寿司店的 450 秒/圈，指导文第 3.4 节已标为假设。若能量一家回转火锅（掐表一圈 + 数座位），这是最值钱的补充数据。
- 寿司店那次只记了座位数和圈时。若再去，值得顺手记：姜片/酱油等公用物件的数量、有没有服务员中途重新摊开、以及饭局后半段是否肉眼可见它们挤在一起。**后者能把文章从纯仿真变成有实证的小研究。**
