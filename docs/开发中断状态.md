# 开发中断状态（2026-08-30 深夜）

> 因流量不足在此处停工。本文件是接手说明：读完它 + 指导文第 5.5 节就能继续，不需要回看聊天记录。
> 施工规格仍以 [`科学小品指导.md`](科学小品指导.md) 为准；本文只记「停在哪、下一步做什么」。

## 一句话状态

模拟核已经跑通并自检通过，**主结论已经拿到且比预期更好**：按实测时标，一顿饭 90 分钟刚好够加汤壶从均匀排布结成一坨（结团指数 0.96）。停工点选在「时标参数需要整体替换、图还没开始画」之前，是个干净的断点。

## 停工时确认过的环境

| 项 | 状态 |
|---|---|
| Node | v22.11.0，可用 `--experimental-strip-types` 直接跑 `.ts`，**不需要装 tsx** |
| npm 依赖 | 无。`package.json` 里没有 dependencies，不必 `npm install` |
| Python | 3.13.2 |
| `.venv` | **已装好** numpy 2.5.2 + matplotlib 3.11.1。下次开工无需联网 |
| git | 在 `main` 分支，但 **一个 commit 都还没有**，全部文件处于 untracked。想留存档就自己提一次 |

复现命令（都不联网）：

```bash
npm run check                 # 模拟核自检
npm run sim                   # 跑全部场景 → data/*.json
npm run sim -- scene-a        # 只跑一个
node --experimental-strip-types --no-warnings src/diag.ts   # 实测时标诊断
./.venv/bin/python -c "import matplotlib; print(matplotlib.__version__)"
```

## 现有文件

| 文件 | 作用 | 状态 |
|---|---|---|
| `src/sim.ts` | **唯一模拟核**。事件驱动、mulberry32 可播种、无 Vue / DOM / Node 依赖 | 可用，自检通过 |
| `src/cli.ts` | Node 批处理，六个场景 → `data/*.json` | 可用，但**时标是旧的**，见下 |
| `src/diag.ts` | 临时诊断脚本，产出了实测时标那批关键数字 | 用完待并入 `check` 后删除 |
| `src/plot.py` | 出图 | **还没写** |
| `web/KettlePlayer.vue` | 播放器 | **还没写** |
| `data/*.json` | 六个场景的输出 | **按旧时标生成，全部作废** |
| `docs/科学小品指导.md` | 施工规格 + 第 5.5 节实测结论 | 已更新 |

## 必须保住的结论（重跑后应能复现）

实测时标：40 座、一圈 **450 秒**（7.5 分钟），火锅 τ=10s=0.0222 圈、λ=0.75 圈⁻¹。

| 时刻 | CV | 结团指数 |
|---|---|---|
| 15 分钟 | 0.86 | 0.39 |
| 30 分钟 | 1.39 | 0.62 |
| **90 分钟（一顿饭）** | **2.15** | **0.96** |
| 180 分钟（晚市） | 2.21 | 0.99 |

- 随机重投对照 CV ≈ 0.81（K=6），完全聚成一坨的上限 \(\sqrt{K-1}\) = 2.24。**15 分钟就越过了随机线。**
- 真实时标比旧假设更快锁定：越过半程 21 分钟 vs 35 分钟。
- 姜片 K=1：等待中位数 315s、九成分位 492s、最长 545s（超过一整圈 450s）。
- 服务员重整（旧时标下测的，需按新时标复核）：不重整 CV 1.92 / 等待 45.2s；每 8 圈重整 → CV 1.28 / 等待 26.8s。

## 未解决的问题

### P0：下次开工立刻做

1. **把 `src/cli.ts` 的时标换成实测值。** 现在是 `LOOP_SECONDS=100`、火锅 `tau: 0.1, lambda: 0.17, duration: 240`。要改成：

   ```
   LOOP_SECONDS = 450
   SCENE_A: tau: 10/450 ≈ 0.0222, lambda: 450/600 = 0.75, duration: 24, frameDt: 0.02
   SCENE_B: tau: 5/450 ≈ 0.0111,  lambda: 450/900 = 0.5,  duration: 24, frameDt: 0.02
   ```

   建议顺手把「秒 → 圈」的换算写成 `sim.ts` 或 `cli.ts` 里的小工具函数（`diag.ts` 里已有 `toLoops` / `rateToLoops` 可以搬），避免以后再手算错一次。改完 `npm run sim` 重跑，作废旧 `data/`。

2. **负荷扫描这张图作废重做。** 稳态 CV 对 λ 几乎不敏感（τ=5s 时 34 倍 λ 变化只从 2.02 到 2.13）。这不是 bug：聚团态是任何正需求率下的吸引子，λ 只决定「多久到」。改画 **「结团所需时间」对 λ 和 τ**，纵轴用「越过随机对照线的时刻」或「到达半程的时刻」。

3. **写 `src/plot.py`。** 只读 `data/*.json`，不做物理。matplotlib 已就绪。需要处理中文字体（macOS 上试 PingFang SC / Heiti SC / Arial Unicode MS，找不到就回退英文标签），否则图里全是方框。

### P1：影响正文说法

4. **叙事弧第 4 步要改。** 指导文第 2 节原稿写「让读者看见不是任何餐厅都会挤成一坨，要够忙」——数据不支持。真实说法是「几乎一定会挤，问题只是多快；正常晚市在头半小时内基本完成」。按项目规矩：改小品，不改数据。

5. **跨 K 只能比归一化指数，这点要在正文里用一句话交代。** 裸 CV 随 K 单调升（K=2 的 0.92 → K=20 的 2.99）纯粹因为上限 \(\sqrt{K-1}\) 在长；归一化指数 CV/\(\sqrt{K-1}\) 反而是降的（0.918 → 0.686）。建议读者面向的纵轴用**平均等待时间**（K=1 时 121s → K=20 时 28s），把结团指数留作机制证据。

6. **场景 B 偏心需求还没看结论。** 第 12 桌 λ×8 时被服务 93 次，但尚未判断姜盒是否真的「粘」在那一带（要看 `heldTimeBySeat` 的空间分布，以及盒子位置轨迹的停留密度）。

### P2：可以再等

7. `src/diag.ts` 里有用的断言并进 `cli.ts` 的 `check` 场景，然后删掉这个临时文件。
8. 播放器 `web/KettlePlayer.vue` 还没开始。规格见指导文第 4.9 节（调参、种子写进 URL、从头模拟、帧缓冲 + 播放头实现快进后退）。
9. 小品正文还没开始。
10. 容量与回壶（10 次、过原点且 ≤40% 用 3τ 补汤）仍是关着的开关，见指导文第 5 节。

## 数据缺口

- **回转火锅的带速没有实测**，目前借用寿司店的 450 秒/圈，指导文里已标为假设。若能量一家回转火锅（掐表一圈 + 数座位），这是目前最值钱的补充数据。
- 寿司店那次只记了座位数和圈时。若再去，值得顺手记：姜片/酱油等公用物件的数量、有没有服务员中途重新摊开、以及肉眼看它们到饭局后半段是否真的挤在一起。**后者能把文章从纯仿真变成有实证的小研究。**
